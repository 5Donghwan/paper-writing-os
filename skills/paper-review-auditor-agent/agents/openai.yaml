interface:
  display_name: "Paper Review Auditor"
  short_description: "Audit claims, baselines, literature, and figures before revision."
  default_prompt: "Use $paper-review-auditor-agent to produce a structured audit from the existing .paper-os state."

policy:
  allow_implicit_invocation: true
