interface:
  display_name: "Paper Refinement Agent"
  short_description: "Revise the paper against reviewer feedback."
  default_prompt: "Use $paper-content-refinement-agent to revise the manuscript using reviewer feedback from .paper-os."

policy:
  allow_implicit_invocation: true
