interface:
  display_name: "Paper Writing OS"
  short_description: "Orchestrate role-based paper writing from state."
  default_prompt: "Use $paper-writing-os to choose the right paper-writing role from the existing .paper-os project state."

policy:
  allow_implicit_invocation: true
