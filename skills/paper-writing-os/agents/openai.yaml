interface:
  display_name: "Paper Writing OS"
  short_description: "Stateful workflow for writing research papers."
  default_prompt: "Use $paper-writing-os to revise this paper using the existing .paper-os project state."

policy:
  allow_implicit_invocation: true
