paper_name: "{{PAPER_NAME}}"
project_dir: "{{PROJECT_DIR}}"
manuscript: "{{MANUSCRIPT}}"
bibliography: "{{BIBLIOGRAPHY}}"
figures_dir: "{{FIGURES_DIR}}"
state_dir: "{{STATE_DIR}}"
venue: "{{VENUE}}"
field: "{{FIELD}}"
created_on: "{{TODAY}}"
workflow:
  stages:
    - scope
    - evidence
    - outline
    - drafting
    - figures
    - audit
    - refinement
ground_truth_files:
  claim_scope: "{{STATE_DIR}}/claim_scope.md"
  evidence_log: "{{STATE_DIR}}/evidence_log.md"
  outline: "{{STATE_DIR}}/outline.json"
  intro_related_work_plan: "{{STATE_DIR}}/intro_related_work_plan.json"
  plotting_plan: "{{STATE_DIR}}/plotting_plan.json"
  illustration_plan: "{{STATE_DIR}}/illustration_plan.json"
  citation_map: "{{STATE_DIR}}/citation_map.json"
  review_audit: "{{STATE_DIR}}/review_audit.json"
  reviewer_feedback: "{{STATE_DIR}}/reviewer_feedback.json"
  worklog: "{{STATE_DIR}}/worklog.json"
  writing_status: "{{STATE_DIR}}/writing_status.md"
