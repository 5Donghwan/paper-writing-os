interface:
  display_name: "Paper Section Writer"
  short_description: "Draft the main body from paper state."
  default_prompt: "Use $paper-section-writing-agent to draft the non-intro sections from the .paper-os plan and evidence."

policy:
  allow_implicit_invocation: true
